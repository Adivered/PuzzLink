import React from 'react';
import RoomLobby from '../components/RoomLobbyContainer';

/**
 * Room Lobby Page component following Single Responsibility Principle
 * Wraps the existing room lobby for the new architecture
 */
export const RoomLobbyPage = () => {
  return <RoomLobby />;
}; 